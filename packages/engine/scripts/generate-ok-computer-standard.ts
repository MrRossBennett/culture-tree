/**
 * Regenerate ok-computer-standard.json (same as generate-fixture with fixed args).
 */
import { runFixture } from "./generate-fixture.ts";

await runFixture("OK Computer — Radiohead", "ok-computer-standard.json");
